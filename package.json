{
  "name": "smartscan-pro",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Privacy-first barcode intelligence and inventory PWA.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build-static.mjs",
    "check": "node --check src/app.js && node --check src/inventory.js && node --check src/product-catalog.js && node --check src/result-actions.js && node --check src/scanner.js && node --check src/storage.js && node --check scripts/dev-server.mjs && node --check sw.js",
    "check:app-shell": "node scripts/verify-app-shell.mjs",
    "test": "node --test",
    "verify": "npm run check && npm run check:app-shell && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
